namespace YuGiOhRandomizer
{
	/// <summary>
	/// An enum of the deck types
	/// Side decks aren't used, so they aren't included (at least not yet)
	/// </summary>
	public enum DeckTypes
	{
		Main,
		Extra
	}
}
